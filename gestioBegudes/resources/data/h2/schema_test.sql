DROP TABLE Begudes IF EXISTS;

CREATE TABLE Begudes (

	ID				BIGINT			NOT NULL,
	NOMBRE			VARCHAR(150)	,
	DESCRIPCION		VARCHAR(250)	,
	PRECIO			DOUBLE			,
	FAMILIA			VARCHAR(10)		,
	FECHA_ALTA		DATE			,
	DESCATALOGADO	BOOLEAN			,
	
	PRIMARY KEY (ID)

);